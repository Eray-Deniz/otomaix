## Turn 1 — 2026-07-11 21:56 UTC

# Codex Adversarial Review

Target: working tree diff
Verdict: needs-attention

No-ship: the spec has a blocking lifecycle/design contradiction around how a draft package can be blind-evaluated before activation, plus rollback semantics that contradict the declared state machine. Coverage: inspected current docs/specs/2026-07-11-sektor-bilgi-paketi.md, docs/active/CURRENT.md, migrations 002/019/021, sectors.py, sector_resolver.py, prompt_builder.py, calendar.py, schemas.py, and repo-wide rg for holiday/package references. Requirement files under /root/otomaix-sosyal-medya-arastirmasi are repo-external intentional and not inspected. Excluded anchors ai.py, caption_generator.py, short_video.py, posts.py, layer_a.py, markalar page are anchor-not-verifiable in this substrate.

Findings:
- [high] Draft packages cannot pass Katman-2 before activation because the only runtime gate loads active packages only (docs/specs/2026-07-11-sektor-bilgi-paketi.md:225-227)
  layer: spec-level. The spec makes Katman-2 blind output the activation input, but the single context gate only loads sector_packages where status='active' (lines 225-227). Later, the pilot sequence says draft insert -> Katman-2 blind evaluation -> operator activation (lines 533-534), and §10.3 says the blind comparison result feeds activation approval. That sequence is unachievable as written: a draft candidate cannot be injected by the runtime path being evaluated. Failure scenario: the first official kuyumculuk draft is written, the team runs the blind sample before activation, every prompt follows the paketsiz path, and the operator either rejects a good package as ineffective or activates without ever testing the candidate package.
  Recommendation: root cause: activation/evaluation lifecycle was specified after the runtime gate was constrained to active-only. minimal fix strategy: add an explicit operator-only preview/evaluation mode that loads one draft package by package_id without changing normal production behavior, or move Katman-2 after activation and redefine rollback criteria. exact affected files+functions: spec §6.1 load_sector_package_context, §10.3, §11 DB discipline, §14 Faz 5; implementation will touch apps/social/backend/app/core/sector_package_context.py::load_sector_package_context and the generation call sites that pass preview package_id. related files w/ same pattern: prompt_builder.build_brand_context, suggest-ideas extraction in ai.py, short_video director paths, posts generation insert path. verification command: after implementation, run pytest tests/regression/ -q plus a DB-backed test proving draft preview injects only when explicit preview package_id is provided and normal brand generation still ignores draft packages. risk if wrong: production can leak unapproved draft packages or, conversely, the pilot never tests the candidate package. fallback: require temporary activation in a staging-only DB, run Katman-2 there, then promote the same frozen row in production.
- [medium] Rollback procedure contradicts the declared package lifecycle (docs/specs/2026-07-11-sektor-bilgi-paketi.md:119-121)
  layer: spec-level. The invariant states lifecycle is draft -> active -> archived (line 119), but the documented rollback says archive the current active row and then make the target version active (lines 120-121). A rollback target is normally already archived, so this requires archived -> active, which the declared lifecycle forbids. Failure scenario: an implementation encodes the stated lifecycle in the trigger and blocks rollback; or it encodes rollback and silently violates the spec's lifecycle invariant. Either outcome weakens K7 because incident recovery depends on unambiguous package state transitions.
  Recommendation: root cause: lifecycle and rollback state machine were written as two incompatible rules. minimal fix strategy: choose and write one state machine, e.g. draft -> active -> archived and archived -> active allowed only by an operator rollback transition in one transaction, with activated_at semantics defined; or create a new active copy from archived content and keep archived immutable. exact affected files+functions: spec §3.2 trigger invariants and §11 DB discipline; implementation migration shared/db/migrations/032_sector_packages.sql trigger function for sector_packages updates. related files w/ same pattern: posts.sector_package_id FK/K7 join logic and any admin activation endpoint/script. verification command: psql migration test transaction that creates v1 active, v2 active, archives v2, reactivates v1 or proves the chosen copy-forward path, then verifies only one active package exists. risk if wrong: failed rollback during bad package incident, or historical package rows mutating in a way that makes post attribution ambiguous. fallback: remove rollback from Faz 1 and document forward-only hotfix activation until a tested transition is specified.
- [medium] Spec claims the Tier 1 hierarchy edit has no behavior effect when DNA is empty, but it deliberately changes the global system prompt (docs/specs/2026-07-11-sektor-bilgi-paketi.md:380-386)
  layer: spec-level. Lines 380-382 say the Tier 1 hierarchy line does not change behavior while DNA fields are absent, but lines 372-378 replace existing system prompt text and lines 383-386 acknowledge the cached system prompt changes before the golden baseline is captured. The current code has a shorter brand-tone priority block in prompt_builder.py:111-113; replacing it with a broader hierarchy can change LLM behavior globally even with no DNA fields. K6 cannot prove no behavior change because the baseline is captured after this intentional prompt mutation. Failure scenario: non-package brands get altered captions from the new hierarchy wording, while the spec reports this as behavior-neutral because byte-exact regression starts after the mutation.
  Recommendation: root cause: prompt-text equality and model behavior were conflated. minimal fix strategy: rewrite the claim to say the Tier 1 prompt change is an accepted global behavior change, baseline-reset before K6, not behavior-neutral; add a small pre/post sampling requirement if the risk needs evidence. exact affected files+functions: spec §9 and §15 criterion 6; implementation apps/social/backend/app/core/prompt_builder.py::build_system_prompt/_SYSTEM_RULES. related files w/ same pattern: K6 golden generation under apps/social/backend/tests/regression/golden. verification command: git diff -- apps/social/backend/app/core/prompt_builder.py plus pytest tests/regression/ -q after baseline capture; optionally run a fixed prompt corpus before/after and record qualitative drift separately from K6. risk if wrong: a broad prompt behavior change ships under the false label of no-op, and regressions in existing brands are dismissed because K6 starts after the change. fallback: defer the DNA hierarchy line to the marka-DNA spec and keep this spec's runtime baseline against the current system prompt.

Next steps:
- Fix the activation/evaluation lifecycle first; it determines whether the chosen runtime-core direction can produce first live value without a hidden staging path.
- Then make the package state machine executable in one SQL trigger spec and align the rollback wording with that trigger.
- Finally, downgrade the Tier 1 no-op claim to an explicit accepted behavior change or defer it.

Claude tahkimi (katman-kapısı): F1 spec-level HIGH — CONFIRMED (fix-required); F2 spec-level technical-medium — CONFIRMED (iç çelişki, fix-required); F3 spec-level technical-medium — CONFIRMED (yanlış iddia, fix-required). PLAN-DEVİR: yok. Otonom Mode A refine turu başlatıldı (cluster'lar: F1=eval-gate-lifecycle, F2=package-state-machine, F3=tier1-claim; tur 1/4).

---
## Turn 2 — 2026-07-11 22:04 UTC

# Codex Adversarial Review

Target: working tree diff
Verdict: needs-attention

No-ship: the spec still has design-layer contradictions around activation ordering, package-layer integrity, and draft preview lifecycle. Coverage: inspected docs/specs/2026-07-11-sektor-bilgi-paketi.md fully, docs/active/CURRENT.md, shared/db/migrations/001_initial_social.sql, 019_sectors_hierarchy.sql, 021_sectors_unified.sql, apps/social/backend/app/routers/sectors.py, apps/social/backend/app/services/sector_resolver.py, apps/social/backend/app/core/prompt_builder.py. Repo-external requirement files were not present intentionally. Excluded backend anchors such as ai.py, caption_generator.py, posts.py, short_video.py, and layer_a.py were not verifiable in this substrate.

Findings:
- [high] Spec gives an activation flow that violates its own one-active-package invariant (docs/specs/2026-07-11-sektor-bilgi-paketi.md:476-482)
  layer: spec-level. §3.2 says the partial unique index allows only one active package and explicitly requires archiving the current active row before activating the target row. §11's canonical process says `ADIM 4 [OPERATÖR] onay → draft'ı active, eskisini archived`, which is the reverse order. An implementer following the process section will hit the unique index whenever a sector already has an active package, blocking normal version rollout and rollback drills. Failure scenario: first package activates, second quarterly package approval follows §11, draft→active fails with a duplicate active package error, leaving the operator with a half-defined manual recovery path.
  Recommendation: Root cause: the state-machine invariant and process summary encode different transition order. Minimal fix strategy: rewrite ADIM 4 and all activation wording to one transaction: lock sector packages for the sector, active→archived first if present, then draft/archived→active with activated_at reset; state that draft→active is valid only when no active row remains. Exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §3.2, §11, §14, §15; later migration trigger and operator activation function/script. Related files w/ same pattern: shared/db/migrations/032_sector_packages.sql when created. Verification command: `rg -n "draft.*active|active.*archived|archived.*active|ADIM 4|aktivasyon" docs/specs/2026-07-11-sektor-bilgi-paketi.md` plus migration tests that activate v1 then v2. Risk if wrong: package rollout fails at approval time or operators invent unsafe manual SQL. Fallback: forbid replacement activation in v1 and document only first-package activation until the activation transaction is implemented.
- [medium] Package-layer FK does not enforce the alt-sector-only invariant the spec relies on (docs/specs/2026-07-11-sektor-bilgi-paketi.md:90-108)
  layer: spec-level. The design says `sector_packages.sector_id` is an alt-sector row and that integrity is enforced at the package layer, but the SQL only has `REFERENCES social.sectors(id)`. Existing migrations show root and child sectors share the same table via nullable `parent_sector_id`; a plain FK accepts root sectors. That makes a root-level package physically valid, contradicting the A′ design where `brands.sector_id` remains the root bucket and `brands.sub_sector_id` is the package path. Failure scenario: a manual draft insert targets `e-ticaret-perakende`, activates successfully, and downstream package lists or context loading now have a root package that can replace root SECTOR_GUIDANCE or pollute the active-package candidate set unless every caller remembers an ad hoc `parent_sector_id IS NOT NULL` filter.
  Recommendation: Root cause: the spec treats an FK to `social.sectors` as semantic integrity for a subtype it does not encode. Minimal fix strategy: make the migration include a trigger or constraint function rejecting `sector_packages.sector_id` whose referenced `social.sectors.parent_sector_id IS NULL`; mirror the same rule for `brands.sub_sector_id` if DB-level protection is desired, or explicitly downgrade package-layer integrity to application-only. Exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §3.2, §3.4, §5; future shared/db/migrations/032_sector_packages.sql; future `load_sector_package_context` and sub-sector list query. Related files w/ same pattern: apps/social/backend/app/routers/sectors.py:list_sectors, apps/social/backend/app/services/sector_resolver.py:resolve_sector. Verification command: migration test inserting package rows for one root sector and one child sector; root insert must fail, child insert must pass. Risk if wrong: root-sector packages silently collapse the root/sub-sector separation and make K6 replacement guarantees hard to interpret. Fallback: if DB trigger is considered too heavy, require all draft inserts through a single validated operator command and say the DB does not enforce this invariant.
- [medium] Draft deletion and preview K7 stamping conflict through the posts FK (docs/specs/2026-07-11-sektor-bilgi-paketi.md:237-245)
  layer: spec-level. §3.2 says draft package rows are deletable, while §6.1 and §15 require preview generations to write the draft package id into `posts.sector_package_id`. Because §3.4 defines a normal FK to `social.sector_packages(id)` and no delete action, any preview post referencing a draft will block deletion of that draft. If the implementation instead adds `ON DELETE SET NULL` to preserve draft cleanup, it destroys the K7 evidence the preview flow depends on. Failure scenario: an operator previews a bad draft, rejects it, then tries to delete the draft; cleanup fails due to preview posts, or the preview audit link is nulled and Katman-2 samples can no longer be tied to the evaluated draft.
  Recommendation: Root cause: preview posts are both disposable samples and FK-backed evidence, but the lifecycle only distinguishes package status. Minimal fix strategy: choose one invariant explicitly: either referenced drafts are not deletable and must be archived/rejected, or preview artifacts live in a separate table, or draft deletion performs explicit preview-post deletion under operator control. Exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §3.2, §3.4, §6.1, §10.4, §15; future migration FK definition; future preview post creation path. Related files w/ same pattern: shared/db/migrations/001_initial_social.sql posts FK defaults show normal NO ACTION behavior. Verification command: migration test creating draft package, preview post referencing it, then attempting draft delete; expected behavior must match the spec. Risk if wrong: rejected drafts accumulate undeletably, or auditability for blind preview samples is lost. Fallback: remove draft deletion from v1 and allow only `draft -> archived/rejected` once previews exist.
- [low] Review metadata under-reports the current re-review state (docs/specs/2026-07-11-sektor-bilgi-paketi.md:6-10)
  layer: spec-level. The prompt states this is re-review turn 2 and that three turn-1 findings were fixed, but frontmatter still says `codex_review_iterations: 1` and `codex_targeted_fixes: 2`. This is a transcription/lifecycle drift in the artifact itself. It will not break runtime behavior, but it can mislead the repo's review bookkeeping and resume/finalization workflows that use these fields as source-of-truth.
  Recommendation: Root cause: targeted fix/review counters were not updated after the second review/fix cycle. Minimal fix strategy: set counters to match the actual process or add an explicit note explaining why the prompt's three closures do not count as targeted fixes. Exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md frontmatter; referenced docs/reviews/codex/2026-07-11-sektor-bilgi-paketi.md when created. Related files w/ same pattern: docs/specs/2026-05-20-spec-claude-codex-command.md defines frontmatter lifecycle expectations. Verification command: `sed -n '1,12p' docs/specs/2026-07-11-sektor-bilgi-paketi.md` and compare to the review log. Risk if wrong: later automation or reviewers believe fewer fixes/iterations happened than actually did. Fallback: keep status pending and mark the review log as authoritative until counters are corrected.

Next steps:
- Fix the spec contradictions before implementation planning, then re-run a line-numbered consistency sweep over §§3, 6, 10, 11, 14, and 15.

Claude tahkimi (katman-kapısı): F4 spec-level HIGH — CONFIRMED (F2 fix'inin kardeş sitesi §11 ADIM 4'te kaçmış; cluster: package-state-machine, tur 2/4). F5 spec-level technical-medium — CONFIRMED (alt-sektör subtype kilidi hiçbir katmanda tanımlı değil; yeni cluster: package-sector-subtype-integrity, tur 1/4). F6 spec-level technical-medium — CONFIRMED (draft silme × preview damgası × FK çelişkisi; yeni cluster: draft-deletion-vs-stamp, tur 1/4). F7 low — REJECTED: sayaç semantiği komut sözleşmesine uygun (codex_review_iterations = TAMAMLANMIŞ refine turları; turn-2 refine'ı bu tahkimden sonra sayaca işlenecek; codex_targeted_fixes=2 Adım 5 sweep fix'leridir). PLAN-DEVİR: yok. Otonom Mode A refine turu 2 başlatıldı (global auto-fix turu 2/8).

---
## Turn 3 — 2026-07-11 22:10 UTC

# Codex Adversarial Review

Target: working tree diff
Verdict: needs-attention

No-ship: the spec mostly honors the selected SYNTHESIS direction, but two spec-level invariants are weak enough to let the design ship with false confidence. Coverage: inspected docs/active/CURRENT.md, docs/specs/2026-07-11-sektor-bilgi-paketi.md current content, apps/social/backend/app/routers/sectors.py, apps/social/backend/app/services/sector_resolver.py, apps/social/backend/app/core/prompt_builder.py, apps/social/backend/app/models/schemas.py, shared/db/migrations/001_initial_social.sql, 002_autoposting.sql, 019_sectors_hierarchy.sql, 031_brand_products_highlight.sql, available frontend onboarding/marka-ayarlari sector references. Requirement/input files under /root/otomaix-sosyal-medya-arastirmasi and research-runs are repo-external intentional, not present. Excluded anchors such as ai.py, posts.py, short_video.py, caption_generator.py, layer_a.py, and markalar/page.tsx were not verifiable in this substrate due to stated secret-value exclusions.

Findings:
- [high] Preview mode can bind an arbitrary draft package to the wrong brand/sector (docs/specs/2026-07-11-sektor-bilgi-paketi.md:247-259)
  layer: spec-level. The single-gate contract normally loads packages through brand.sub_sector_id, but preview_package_id is defined as an override that loads a draft package without stating the draft must belong to the brand's sub_sector_id or root sector. That breaks the core assignment invariant under the exact path used for Katman-2 approval: an operator/internal caller can preview an ayakkabici package through a kuyumcu brand, get a sector_package_id stamp for the wrong draft, and make the blind evaluation/activation decision on a false sample. The issue is not merely missing choreography; the spec's stated gate invariant is incomplete for the preview trust boundary.
  Recommendation: root cause: preview_package_id bypasses the brand→sub_sector package selection invariant. minimal fix strategy: make the spec binding say preview loads only draft packages where sector_packages.sector_id = brand.sub_sector_id, and rejects otherwise; if sub_sector_id is null, preview must require an explicit operator-selected sub_sector that also matches the draft and is stamped in evidence. exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §6.1/§10.3/§15; future apps/social/backend/app/core/sector_package_context.py load_sector_package_context. related files w/ same pattern: apps/social/backend/app/routers/sectors.py and app/services/sector_resolver.py are assignment boundary code; short_video/posts/ai anchors not verifiable in substrate. verification command: add a regression for preview wrong-sector rejection and run cd apps/social/backend && pytest tests/regression/ -q plus the DB/API test that calls load_sector_package_context with mismatched brand.sub_sector_id and preview_package_id. risk if wrong: pilot evidence and K7 stamps can certify the wrong package, causing cross-sector prompt injection and misleading rollback/audit history. fallback: disable preview_package_id until the sector-match guard is implemented.
- [medium] Research provenance is not an enforceable activation invariant after rejecting the full vertical slice (docs/specs/2026-07-11-sektor-bilgi-paketi.md:93-105)
  layer: spec-level. The spec's chosen direction defers the command-family mechanism, which is acceptable only if invariant/file contracts preserve the research→review→synthesis evidence chain. But sector_packages.run_id is just nullable TEXT, the schema has no FK or activation invariant tying an active package to required research/review/synthesis artifacts, and the success criteria only test package state transitions. A manually/script-inserted draft can be activated with null or typoed run_id and no archived artifacts while still satisfying §15, contradicting the stated goal that packages are research+referee-vetted and weakening the rationale for dropping the full vertical slice.
  Recommendation: root cause: the deferred pipeline keeps file contracts, but activation does not require a durable package↔run evidence binding. minimal fix strategy: either make run_id NOT NULL for sector_packages and specify activation checks that the run has at least research, review, and synthesis artifact rows for the same sector/run, or add a separate package_artifact_links table and require it before draft→active. exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §3.1, §3.2, §11, §15; future shared/db/migrations/032_sector_packages.sql triggers/constraints and operator activation path. related files w/ same pattern: external research-runs files are repo-external intentional; no implementation file exists yet. verification command: add DB tests for draft/active activation with null run_id, nonexistent run_id, and incomplete artifact kinds; run the migration test suite plus cd apps/social/backend && pytest tests/regression/ -q. risk if wrong: active packages can become unreviewable content blobs with frozen status and K7 stamps but no provable research lineage. fallback: require the first manual referee run to write a signed activation checklist file referenced by a non-null brief_ref/run_id before allowing activation.

Next steps:
- Add the preview sector-match invariant to §6.1, §10.3, and §15 before implementation planning.
- Add an enforceable package↔research-run provenance invariant, or explicitly document that activation without DB-verifiable provenance is an accepted risk.

Claude tahkimi (katman-kapısı): F8 spec-level HIGH — CONFIRMED (preview güven sınırı eksik: sektör-eşleşme şartı yok; cluster: preview-gate [F1 mirası], tur 2/4). Fix: preview yalnız sector_id == brand.sub_sector_id draft'ını yükler; atamasız markada RED. F9 spec-level technical-medium — CONFIRMED (provenance aktivasyonda zorlanmıyor; yeni cluster: activation-provenance, tur 1/4). Fix: run_id NOT NULL + her →active geçişinde aynı (sector, run_id) için en az bir kind='synthesis' artefaktı şartı (DB minimumu bilinçli synthesis-varlığı; tam hat disiplini §11 sözleşmesi — dürüst sınır belgelendi). PLAN-DEVİR: yok. Otonom Mode A refine turu 3 (global auto-fix turu 3/8).

---
## Turn 4 — 2026-07-11 22:16 UTC

# Codex Adversarial Review

Target: working tree diff
Verdict: needs-attention

No-ship: the SYNTHESIS direction is defensible versus both rejected alternatives, but the current spec overclaims two safety contracts. Coverage: inspected docs/active/CURRENT.md; full docs/specs/2026-07-11-sektor-bilgi-paketi.md; related substrate files apps/social/backend/app/core/prompt_builder.py, apps/social/backend/app/services/sector_resolver.py, apps/social/backend/app/routers/sectors.py, apps/social/backend/app/services/upload_post.py, shared/db/migrations/{001,019,021,031}*. External research/task files are not present by design. Excluded backend anchors were not inspected; treated as anchor-not-verifiable-in-substrate, not missing.

Findings:
- [high] Preview posts are asserted non-publishable, but the spec does not bind any publish guard (docs/specs/2026-07-11-sektor-bilgi-paketi.md:271-272)
  layer: spec-level. The spec says preview generations carry the draft package stamp and that “preview post'ları yayınlanmaz” at lines 271-272, but the design only gates how preview context is loaded. It does not require a post-level preview/nonpublishable state, DB constraint, scheduler exclusion, or publish-service guard. In the inspected substrate, publish_post() publishes any post with output_url and platforms after setting status='publishing' (apps/social/backend/app/services/upload_post.py:396-424), and the posts schema has only generic status/platform/scheduled fields (shared/db/migrations/001_initial_social.sql:89-106). Failure scenario: an operator preview creates a normal ready/scheduled post stamped with a draft sector_package_id; a manual or scheduled publish path can publish it before activation/rejection, making a draft package externally visible and undermining the claimed draft isolation/K7 evidence boundary. This is not a plan detail because the spec asserts a safety invariant without naming the data-state invariant that makes it true.
  Recommendation: Root cause: preview is modeled as a context-loader option, not as a persisted post lifecycle state. Minimal fix strategy: bind a spec invariant that preview-generated posts are physically non-publishable, e.g. posts.preview_package_id/is_preview or status='preview' with CHECK/trigger/service guards, and exclude preview rows from scheduler/manual publish paths. Exact affected files+functions for implementation: docs/specs/2026-07-11-sektor-bilgi-paketi.md §3.4/§6.1/§10.4/§15; migration 032 posts additions/triggers; apps/social/backend/app/services/upload_post.py::publish_post; scheduler/internal publish entrypoints; posts creation path in excluded posts.py. Related files with same pattern: shared/db/migrations/001_initial_social.sql generic posts lifecycle. Verification command: pytest tests covering preview post creation then publish/schedule rejection, plus K6 fixture showing preview-only draft does not affect normal generation. Risk if wrong: draft content can be published and archived/rejected package evidence becomes misleading. Fallback: if no DB state is added, remove the absolute “preview posts are not published” claim and require preview outputs to be generated outside social.posts.
- [medium] Research pipeline contract loses the actual research step in the numbered flow (docs/specs/2026-07-11-sektor-bilgi-paketi.md:507-514)
  layer: spec-level. The chosen decision says the research-pipeline command family is deferred only at mechanism level while invariant/file contracts are bound here, and §3.2 explicitly says full pipeline discipline remains in §11. But §11’s canonical numbered flow starts with brief-doctor, then jumps to Denetçi-1/Denetçi-2, synthesis, draft, activation (lines 507-514). The three Deep Research outputs are mentioned as files at lines 518-522, yet no flow step requires producing or validating them before review/synthesis, and §15 only requires synthesis presence for activation. Failure scenario: the first manual run follows the written ADIM list, writes a synthesis artifact and draft, passes the DB provenance lock, and activates without the promised 3-source research basis. This is a transcription/contract drift, not a request for mechanism detail: the missing invariant falsifies the stated “3 araç + 2 denetçi + sentez” discipline.
  Recommendation: Root cause: flow numbering appears copied from the referee/review phase and omits the upstream KAYNAK-1/2/3 research production/acceptance step. Minimal fix strategy: revise §11 to include an explicit ADIM for 3 independent research artifacts before brief-doctor/review, or rename/reorder steps so KAYNAK-1/2/3 existence is a bound invariant; add §15 success criteria requiring 3 kind='research', 2 kind='review', and 1 kind='synthesis' artifacts for the run even if DB activation only enforces synthesis. Exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §11 and §15; later command-family plan file docs/plans/<tarih>-sektor-paket-komut-ailesi.md; migration 032 may optionally add non-DB test fixtures, not necessarily DB constraints. Related files with same pattern: sector_research_artifacts schema at §3.1 already has kind='research'/'review'/'synthesis' but no process acceptance criterion. Verification command: documentation drift check plus an implementation test/script that rejects an activation packet missing the expected artifact counts before operator approval. Risk if wrong: packages can ship with a veneer of provenance while lacking the intended independent research inputs. Fallback: explicitly downgrade §11 to “synthesis-only DB minimum plus manual process convention” and stop claiming full pipeline discipline is bound in this spec.

Next steps:
- Add a spec-level nonpublishable preview-post invariant before implementation planning.
- Repair §11 flow/§15 criteria so the deferred command family still has a complete file-contract for research, review, and synthesis artifacts.

Claude tahkimi (katman-kapısı): F10 spec-level HIGH — CONFIRMED (yayınlanmazlık iddiası invariant'sız; cluster: preview-gate, tur 3/4). Fix: yayın kilidi — sector_package_id dolu post, paket active değilse publishing/published geçişinde trigger RAISE (kolon eklenmez; scheduler dahil tüm yollar kapsanır). F11 spec-level technical-medium — CONFIRMED (akışta araştırma adımı eksik — transcription drift; yeni cluster: pipeline-flow-contract, tur 1/4). Fix: §11 akışına ADIM A (3 bağımsız Deep Research → KAYNAK-1/2/3) + §15 süreç kriteri (normal tur: 3 research + 2 review + 1 synthesis; eleme/acil istisnası EK-E/decision_log ile belgeli — DB minimumu synthesis kalır). PLAN-DEVİR: yok. Otonom Mode A refine turu 4 (global auto-fix turu 4/8).

---
## Turn 5 — 2026-07-11 22:21 UTC

# Codex Adversarial Review

Target: working tree diff
Verdict: needs-attention

No-ship at spec layer. The SYNTHESIS direction is defensible versus both dropped alternatives, but the current spec breaks its own K7 evidence model around preview drafts, and its phase order can create a package-capable window before K7 is enforced. Coverage: inspected docs/specs/2026-07-11-sektor-bilgi-paketi.md all 664 lines directly, docs/active/CURRENT.md, prompt_builder.py, sector_resolver.py, sectors.py, migration 031. Requirement/input research files are not present, repo-external intentional. Excluded anchors such as ai.py, posts.py, short_video.py, caption_generator.py were not verifiable in this substrate.

Findings:
- [high] K7 preview stamps can point at mutable draft content, so the stamp may not identify what generated the post (docs/specs/2026-07-11-sektor-bilgi-paketi.md:119-125)
  layer: spec-level. Failure scenario: an operator previews a draft package, producing posts stamped with posts.sector_package_id. The spec then allows the same draft row to remain mutable because freeze only applies when status != 'draft' (lines 119-121). Since previewed drafts are only protected from DELETE when referenced (lines 123-125), the operator or script can UPDATE content/decision_log/run_id after preview and before activation. Once activated, the publish lock accepts the same post because the referenced package is now active, but the package row no longer necessarily contains the content used to generate the preview post. This falsifies the K7 claim that the FK is a version/evidence stamp for Katman-2 sample matching and bad-output traceability.
  Recommendation: Root cause: draft mutability and preview stamping are combined without a 'previewed draft is frozen' invariant. Minimal fix strategy: add a DB-level trigger/invariant that blocks content/version/schema_version/sector_id/decision_log/run_id UPDATE on any sector_packages row referenced by posts.sector_package_id, or introduce a distinct immutable preview snapshot/version row before preview. Exact affected files+functions: update this spec in sections 3.2, 3.4, 6.1, 10.4, 15; planned migration shared/db/migrations/032_sector_packages.sql trigger for sector_packages UPDATE; planned post generation paths that write posts.sector_package_id; sector_package_context preview load path. Related files w/ same pattern: publish-lock trigger in planned 032 and regression fixtures under apps/social/backend/tests/regression. Verification command: add migration tests, then run `pytest apps/social/backend/tests/regression/ -q` plus DB tests proving referenced draft content UPDATE fails, unreferenced draft UPDATE succeeds if intended, draft->active still succeeds, and draft-damgalı post publishes only after activation. Risk if wrong: published or evaluated posts can be attributed to a package version that did not generate them, poisoning blind review and future quality loops. Fallback: do not allow preview posts to become publishable; require regeneration after activation.
- [medium] Implementation phases can expose package generation before the K7 stamp exists (docs/specs/2026-07-11-sektor-bilgi-paketi.md:609-612)
  layer: spec-level. The spec says K7 is non-deferrable because skipping it cannot be recovered later (lines 507-513), but the phase plan adds injection surfaces in Faz 2, brand assignment in Faz 3, and only then writes posts.sector_package_id in Faz 4 (lines 609-612). If phases are shipped or tested as increments, Faz 3 creates a package-capable production path whose posts can be generated with sector package context but no stamp. That contradicts the stated runtime-core decision that K7 closes in this spec and creates exactly the unrecoverable provenance gap the spec warns against.
  Recommendation: Root cause: phase ordering treats K7 as a later implementation step even though it is part of the first package-capable runtime invariant. Minimal fix strategy: move K7 stamp writing and publish-lock migration into the same phase as the first surface that can generate with package context, or state an explicit release gate that Faz 2-3 cannot be deployed with package access enabled until Faz 4 lands. Exact affected files+functions: update spec section 14 and success criteria section 15; planned post creation/update functions that persist generated posts; planned migration 032 posts.sector_package_id and publish trigger. Related files w/ same pattern: apps/social/backend/app/core/prompt_builder.py package injection surface; excluded anchors posts.py/short_video.py/ai.py/caption_generator.py not verifiable in substrate. Verification command: add an integration/regression test that any package-context generation creates a non-NULL sector_package_id in the same transaction, then run `pytest apps/social/backend/tests/regression/ -q`. Risk if wrong: early pilot or partial rollout produces unstamped package outputs, making later bad-output triage and Katman-2 sample pairing unrecoverable. Fallback: keep package assignment and preview endpoints disabled behind an operator-only flag until K7 persistence is live.

Next steps:
- Fix the K7 draft-freeze invariant before implementation planning.
- Reorder or gate phases so no package-conditioned output can exist without sector_package_id.
- Non-blocking transcription note: frontmatter still says codex_review_iterations=4 and codex_review_status=pending while this request describes turn 5 final closure verification; update review metadata when closing.

Claude tahkimi (katman-kapısı): F12 spec-level HIGH — CONFIRMED (önizlenmiş draft mutable → K7 kanıt zinciri delik; cluster: draft-mutation-vs-stamp [F6 mirası], tur 2/4). Fix: damga-donması — posts referanslı paket satırı statüden bağımsız içerik-donmuş; düzeltme = yeni draft sürümü. F13 spec-level technical-medium — CONFIRMED (faz sırası K7'siz paket-üretim penceresi açıyor — spec'in kendi 'atlanamaz' beyanıyla çelişki; yeni cluster: phase-ordering, tur 1/4). Fix: K7 damga yazımı Faz 2'ye (ilk paket-bağlamlı yüzeyle aynı faz) + release-gate; eski Faz 4 birleşir, Faz 5→4. Preview-gate cluster'ı turn 5'te bulgu almadı → KAPANDI. Codex'in non-blocking frontmatter notu = F7 reprise → aynı gerekçeyle reddedildi (sayaç semantiği komut sözleşmesine uygun; pending, approve edilene dek doğru değer). PLAN-DEVİR: yok. Otonom Mode A refine turu 5 (global auto-fix turu 5/8).

---
## Turn 6 — 2026-07-11 22:26 UTC

# Codex Adversarial Review

Target: working tree diff
Verdict: needs-attention

No-ship: the spec still has design-layer drift in publish-state coverage and content-integrity enforcement. Coverage: inspected docs/specs/2026-07-11-sektor-bilgi-paketi.md fully, docs/active/CURRENT.md, shared/db/migrations/001_initial_social.sql, 016_post_publications.sql, 019_sectors_hierarchy.sql, migration list through 031, and apps/social/backend/app/services/upload_post.py. Repo-external research/task files were not present intentionally. Excluded backend anchors listed by the substrate note were not verifiable in this substrate.

Findings:
- [high] Publish lock omits the real partially-published terminal state (docs/specs/2026-07-11-sektor-bilgi-paketi.md:201-205)
  layer: spec-level. The spec claims a non-null sector_package_id is physically non-publishable unless the package is active, but the DB trigger is specified only for transitions to `publishing`/`published`. Current repo status semantics include `partially_published` as a published outcome, and upload_post.py can set that after at least one platform succeeds. Any manual/admin path, retry bug, or future scheduler path that writes `partially_published` directly would bypass the stated physical invariant while still representing user-visible publication. This falsifies the spec's stronger claim that all publish states are DB-blocked, not just the happy-path service transition through `publishing`. Evidence outside the spec: shared/db/migrations/016_post_publications.sql lines 4-7 define `partially_published`; apps/social/backend/app/services/upload_post.py lines 389-392 and 536-548 use it as a publish result.
  Recommendation: root cause: the invariant was written around the service's intermediate `publishing` step instead of the full post status domain. minimal fix strategy: define the trigger over every status that implies external publication: `publishing`, `published`, and `partially_published`, and make §15 include a direct `draft-damgalı -> partially_published` rejection test. exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §3.4 and §15; implementation later in shared/db/migrations/032_sector_packages.sql posts trigger. related files w/ same pattern: shared/db/migrations/016_post_publications.sql status comments; apps/social/backend/app/services/upload_post.py `publish_post_to_platforms`. verification command: after implementation, run `pytest apps/social/backend/tests/regression/ -q` plus a DB migration test that attempts inactive-package posts status updates to `publishing`, `published`, and `partially_published`. risk if wrong: draft or archived package content can be tied to a post that is externally published, breaking K7 evidence and operator preview isolation. fallback: if `partially_published` is intentionally unreachable except after `publishing`, downgrade the claim from physical DB invariant to service-path invariant, but that weakens the stated F10 closure.
- [medium] Content schema validity is claimed as impossible to bypass, but the spec only binds Pydantic validation (docs/specs/2026-07-11-sektor-bilgi-paketi.md:178-179)
  layer: spec-level. The spec says invalid package `content` cannot enter the DB, yet `content` is just JSONB in the proposed schema and the enforcement mechanism is a Pydantic model at draft-write time. The official first run explicitly allows manual/script DB writes, and activation's DB minimum only checks for a synthesis artifact, not schema validity. A bad script or privileged manual insert can create an invalid draft and later activate it if the provenance row exists, violating the stated invariant before implementation details even begin. This is not a request for more choreography; it is a mismatch between an absolute data-integrity claim and the enforcement boundary described by the spec.
  Recommendation: root cause: the spec uses application-layer validation language to make a database-level impossibility claim. minimal fix strategy: either add DB-enforced JSON shape validation in migration 032, or narrow the claim to 'all sanctioned writers must call the Pydantic validator' and add a named script/entrypoint contract for manual inserts. exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §3.3, §11 DB discipline, §15 success criteria; implementation later in the draft insert path and/or migration trigger/check. related files w/ same pattern: first official manual/script flow at lines 568-572. verification command: add a migration/integration test that tries to insert malformed `content` directly and confirms the intended result, either DB rejection or documented non-goal. risk if wrong: malformed package JSON can reach active runtime prompts, causing missing prompt blocks, invalid holiday keys, or crashes in package-capable generation. fallback: if DB validation is too costly, require a single repository-owned import script and explicitly state direct SQL writes are unsupported and must be blocked by DB privileges.
- [medium] The active-package cap decision is recorded but not bound to any invariant or acceptance check (docs/specs/2026-07-11-sektor-bilgi-paketi.md:581-593)
  layer: spec-level. The operator decision says active package cap is <=5, but the data model only enforces one active package per sector and the success criteria never test a global cap. As written, the activation path can create an active package for every sub-sector, so the spec does not honor the rollout cap it records. This is a design-choice drift, not a plan-detail gap, because the same spec fully specifies activation invariants and release gates while leaving this operator constraint unenforced and untestable.
  Recommendation: root cause: the cap was captured as a decision log row but not promoted into the activation contract. minimal fix strategy: decide whether <=5 is a hard invariant or an operational pilot note. If hard, add a DB trigger or activation service guard and a §15 test for the sixth active package rejection. If operational, move it out of the decisions table into scope notes and state it is not enforced. exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §3.2 activation rules, §12, §14, §15; implementation later in shared/db/migrations/032_sector_packages.sql or the activation command. related files w/ same pattern: §3.2 only has `one_active_package_per_sector`, not global active count. verification command: migration/integration test that activates five package rows then attempts a sixth and asserts the chosen behavior. risk if wrong: rollout can silently exceed the intended operational blast radius, weakening K6/K7 evidence and cost/quality controls. fallback: explicitly redefine the cap as a manual pilot target rather than a shippable invariant.

Claude tahkimi (katman-kapısı): F14 spec-level HIGH — CONFIRMED (yayın kilidi partially_published'ı kapsamıyor; cluster: preview-gate/yayın-sınırı, tur 4/4 → 4-TAVAN DOLDU). F15 spec-level technical-medium — CONFIRMED (mutlak content-doğrulama iddiası ↔ app-layer enforcement uyumsuzluğu; İlke 3 düzeltmesi: iddia daraltılır + tek repo-sahipli import script sözleşmesi; yeni cluster: content-validation-claim, tur 1/4). F16 spec-level medium — CONFIRMED, çözüm honest-labeling: ≤5 tavanı operatör kararının orijinal bağlamına uygun olarak 'operasyonel pilot hedefi (zorlanmaz, ilk turda revize)' diye etiketlenir (yeni cluster: cap-labeling, tur 1/4). OTONOM DÖNGÜ DURDU: preview-gate cluster 4-tavan → kullanıcı DUR raporu (komut sözleşmesi). Fix'ler kullanıcı kararına kadar UYGULANMADI.

---
## Turn 7 — 2026-07-11 22:36 UTC

# Codex Adversarial Review

Target: working tree diff
Verdict: needs-attention

No-ship: I found spec-level drift in the publish-lock invariant and the content-import contract. Coverage: inspected docs/specs/2026-07-11-sektor-bilgi-paketi.md directly, AGENTS.md, docs/active/CURRENT.md, backend/frontend AGENTS, non-excluded anchors in sectors.py, sector_resolver.py, prompt_builder.py, migrations 001/016/019/021, upload_post.py, migration list/status. Repo-external research/task files were not present by design; excluded backend anchors were not verifiable in this substrate.

Findings:
- [high] Publish lock is written as an UPDATE-only transition guard, so the physical invariant can be bypassed by an already-published insert (docs/specs/2026-07-11-sektor-bilgi-paketi.md:206-215)
  layer: spec-level. The spec claims a post with non-NULL sector_package_id whose package is not active is physically non-publishable, including preview draft posts, but the binding mechanism is only a `posts` BEFORE UPDATE trigger on status transitions. That does not reject an INSERT that creates a row already in `publishing`, `published`, or `partially_published` with a draft/archived package. The current success criteria also only test transitions, so this bypass would be invisible. Inference: normal app paths likely create posts before publish, but the stated invariant is DB-physical and should survive direct/alternate sanctioned writers, retries, imports, and future code paths.
  Recommendation: root cause: invariant says physical non-publishability, but the trigger scope is narrower than the invariant. minimal fix strategy: change the spec and migration requirement to `BEFORE INSERT OR UPDATE OF status, sector_package_id` and reject any NEW row whose `sector_package_id IS NOT NULL`, `NEW.status IN ('publishing','published','partially_published')`, and joined package status is not `active`; add tests for illegal INSERT and illegal UPDATE. exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §3.4/§6.1/§15; future shared/db/migrations/032_sector_packages.sql trigger function, e.g. `social.enforce_post_sector_package_publishable()`. related files w/ same pattern: apps/social/backend/app/services/upload_post.py `publish_post` sets `publishing` then final status; shared/db/migrations/016_post_publications.sql documents the publish status domain. verification command: after implementing migration, run a DB test that inserts a draft package, then attempts `INSERT INTO social.posts (..., status, sector_package_id) VALUES (..., 'published', draft_id)` and separately UPDATEs ready->publishing/published/partially_published; all illegal cases must raise. risk if wrong: draft-preview content can be published or imported as already published, breaking K7 evidence and the operator-only preview boundary. fallback: if INSERT-time blocking is intentionally out of scope, narrow every physical/non-publishable claim to transition-only and explicitly mark already-published direct inserts unsupported.
- [medium] The spec says all draft writes use a named repo-owned import entrypoint, but the entrypoint is not actually named (docs/specs/2026-07-11-sektor-bilgi-paketi.md:178-184)
  layer: spec-level. The accepted F15 closure depends on a single sanctioned writer so app-layer Pydantic validation is auditably mandatory. The spec repeats that all manual pilot and future command-family draft writes go through one repo-owned import script, but it never gives the script path, command name, or callable entrypoint. That leaves the core content-validation contract unenforceable at the spec/file-contract layer: two implementers can create two different writers and still claim they followed the text.
  Recommendation: root cause: the design chose app-layer validation, but omitted the concrete single-writer file contract that makes that boundary auditable. minimal fix strategy: name the entrypoint in the spec, e.g. `apps/social/backend/scripts/import_sector_package.py` with one public command/function for draft import, and state that command family and manual pilot must call it rather than duplicate insert logic. exact affected files+functions: docs/specs/2026-07-11-sektor-bilgi-paketi.md §3.3 and §11; future `apps/social/backend/scripts/import_sector_package.py` and Pydantic model module. related files w/ same pattern: §11 lines 576-581 repeats the unnamed import-script dependency; §15 should include a verification item that direct imports fail or are absent and the named script validates malformed content. verification command: `rg -n "import_sector_package|sector_packages.*INSERT|SectorPackageContent" apps/social/backend shared/db docs/specs/2026-07-11-sektor-bilgi-paketi.md` after implementation, plus a test invoking the named script with invalid `ozel_gun` key/content length. risk if wrong: validation becomes a convention rather than a contract, allowing malformed JSONB packages to reach draft/preview and later active because DB-level JSON schema validation was deliberately not built. fallback: if the script path must remain plan-owned, remove the claim that this spec binds a named entrypoint and label the single-writer guarantee unresolved until the plan names it.

Next steps:
- Fix the two spec defects before shipping the spec: broaden the publish-lock trigger contract to INSERT+UPDATE and name the single import entrypoint. I did not inspect repo-external research/task files because the prompt states they are intentionally outside this scan substrate; excluded code anchors were treated as not verifiable rather than missing.

Claude tahkimi: F17 spec-level HIGH — CONFIRMED (yayın kilidi UPDATE-only; INSERT-published + damga-yönlendirme bypass'ı; cluster: preview-gate/yayın-sınırı — 4-tavan SONRASI kullanıcı-onaylı turda geldi). F18 spec-level technical-medium — CONFIRMED (import entrypoint adlandırılmamış → F15 sözleşmesi denetlenemez; yeni cluster: import-entrypoint-naming). Kullanıcı anlaşması (turn-6 DUR kararı) gereği OTOMATİK FIX YOK — unresolved listesiyle kullanıcıya dönüldü; karar-sonrası verification yasağı uyarınca yeni otonom tur açılmadı.

---
## Kapanış — 2026-07-11 22:43 UTC

KULLANICI KARARI (turn-7 DUR raporu): "Uygula + doğrulamasız onayla". F17 fix uygulandı (yayın kilidi BEFORE INSERT OR UPDATE OF status, sector_package_id kapsamına genişletildi — zaten-published INSERT + damga-yönlendirme bypass'ları kapalı; §3.4/§6.1/§15). F18 fix uygulandı (tek import giriş noktası ADLANDIRILDI: apps/social/backend/scripts/import_sector_package.py — §3.3/§11/§15; komut ailesi insert mantığını kopyalamaz, script'i çağırır). Her iki fix Codex'in turn-7 structured-recommendation'ıyla birebir. NOT (dürüst etiket): turn-7 fix'leri Codex re-review'ıyla DOĞRULANMADI — kullanıcı kararı (karar-sonrası verification yasağı; ek doğrulama turu ancak kullanıcı isterse). Unresolved bulgu YOK (15 confirmed bulgunun 15'i fix edildi; 2 bulgu tahkimde reddedildi: F7 + F7-reprise sayaç semantiği). Sayaç kapanışı: codex_review_iterations=7, codex_targeted_fixes=2, global auto-fix turu 6/8 (turn-7 fix'leri kullanıcı-onaylı kapanış işlemi).
